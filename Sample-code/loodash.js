// Import lodash library
const _ = require('lodash');

// Use lodash to work with an array of numbers
const numbers = [1, 2, 3, 4, 5, 6];

// Use lodash's shuffle method to shuffle the array
const shuffledNumbers = _.shuffle(numbers);

// Log the shuffled array to the console
console.log('Shuffled Numbers:', shuffledNumbers);
